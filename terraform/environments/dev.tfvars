#   per-environment values now live under each split config —
#   infra/terraform/core/environments/*.tfvars   (infrastructure provisioning)
#   infra/terraform/app/environments/*.tfvars    (application deployment)
